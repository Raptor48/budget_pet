# Budget Pet V2 — development / CI dependencies.
#
# Use locally and in CI (pytest, coverage, HTTP client for FastAPI
# TestClient). Not shipped in the production Docker image.
#
# Install with:
#     pip install -r requirements-dev.txt
# which transitively pulls in the runtime requirements via `-r` below.

-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0

# FastAPI TestClient (used by tests/ and tests/v2/).
httpx==0.28.1
httpcore==1.0.9
